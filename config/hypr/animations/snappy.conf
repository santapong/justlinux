# animation profile: snappy — short, no overshoot, work-mode
animations {
    enabled = true
    bezier = snap, 0.2, 0.9, 0.1, 1.0
    animation = windows, 1, 2, snap
    animation = fade, 1, 2, default
    animation = workspaces, 1, 2, snap
}
