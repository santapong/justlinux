[package]
name = "hyprdesk"
version = "0.1.0"
edition = "2021"
description = "The fleet contracts in Rust — parses the SAME files lib/hyprdesk parses, so the two implementations cannot drift without a visible symptom"

[dependencies]
fs2 = "0.4"
serde_json = "1"

[dev-dependencies]
fontdue = "0.9"
